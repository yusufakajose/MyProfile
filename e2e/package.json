{
  "name": "linkgrove-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Playwright end-to-end tests for LinkGrove",
  "scripts": {
    "pretest": "playwright install --with-deps",
    "test": "playwright test",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.0",
    "@types/node": "^20.11.30",
    "@axe-core/playwright": "^4.8.3"
  }
}


